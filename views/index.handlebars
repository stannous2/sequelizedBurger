<!--Jumbotron-->
<div class="jumbotron text-center bg-primary text-white">
  <h2>Welcome to The Home Awesome Burgers!!!</h2>
  <img src="assets/img/cheese-burger.jpeg">
</div>

{{!--
<h1>Enjoy Da Burger!</h1>

<h2>New Burgers!</h2> --}}

<ul>
  {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block devoured=false}} {{/unless}} {{/each}}
</ul>

<h2>Burgers devoured!</h2>

<ul>
  {{#each burgers}} {{#if devoured}} {{> burgers/burger-block devoured=true}} {{/if}} {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    {{!--
    <label for="bu">Burger Name:</label> --}}
    <input type="text" id="bu" name="name">
  </div>

  <button type="submit">Submit</button>
</form>